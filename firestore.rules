
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    /**
     * 严禁程序与 Firebase 云端进行任何临床或配置数据交换。
     * 系统已切换至纯 MySQL 8.4 核心驱动。
     */
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
