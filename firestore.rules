
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    /**
     * 禁用所有临床数据同步。
     * 系统已切换至 MySQL 核心驱动，Firestore 不再存储任何业务数据。
     */
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
